import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Quality Control Platform',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
